name: chronotherapy_app
description: A circadian light analyzer for chronotherapy
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Sensors
  sensors_plus: ^4.0.2
  light: ^3.0.1
  
  # Device info
  device_info_plus: ^9.1.1
  permission_handler: ^11.1.0
  
  # Charts
  fl_chart: ^0.66.0
  
  # Storage
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  
  # State management
  provider: ^6.1.1
  
  # Utils
  uuid: ^4.3.3
  intl: ^0.19.0
  
  # Export
  csv: ^6.0.0
  pdf: ^3.10.7
  
  # Math
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/data/spd_presets.json
    - assets/data/device_profiles.json
    - assets/data/prc_table.json